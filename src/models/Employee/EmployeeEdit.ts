export type EmployeeEdit = {
    first_name?: string,
    last_name?: string,
    phone_number?: string,
    email?: string,
    job_type?: number,
    start_date?: Date,
    birth_date?: Date,
    id: number,
}